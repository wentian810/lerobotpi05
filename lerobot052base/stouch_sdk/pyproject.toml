[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "stouch_sdk"
version = "0.1.0"
description = "Stouch tactile sensor SDK"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["stouch_sdk*"]
